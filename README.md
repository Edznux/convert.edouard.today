# FIT to GPX

Convert Garmin `.FIT` files to `.GPX` — entirely offline, in your browser.

Also hosted at [convert.edouard.today](https://convert.edouard.today).

## Run locally

```
python3 -m http.server
```

Open `http://localhost:8000`. Works offline after first load.

## Tests

```
node test.js
```
